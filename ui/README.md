# FormaToG UI

### Get started

1. See `.nvmrc` for the right node version. If you have `nvm` just use

   ```
   nvm use
   ```

2. Install dependencies

   ```
   npm i
   ```

3. Run on your localhost

   ```
   npm run dev
   ```
